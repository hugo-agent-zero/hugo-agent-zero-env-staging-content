# Hugo
/public/
/resources/
.hugo_build.lock

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Env / secrets (if added later)
.env
.env.*

# Local masters / scratch — rename to *.orig to keep off GH and out of the build
*.orig
